/*
	1.cm  (Damon Barrett 0834104)
	
	Main function calling a function that computes that avg of an array.
	No Errors.
*/

/* Compute average of array with length len */
int arrayAverage(int array[], int len) 
{
	int itr;
	int sum;

	itr = 0;
	sum = 0;

	while (itr < len) {
		sum = sum + array[i];
		itr = itr + 1;
	}

	return (sum/len);
}

void main(void) 
{
	int arr[4];
	int factor;

	factor = 3;

	arr[0-0] = factor * 10;
	arr[1] = factor + (factor/10);
	arr[2 * 1] = ((2/3)-5) * factor;
	arr[5 + 6 - 7] = 43;

	printf(factor);
	printf( arrayAverage(arr, 4) );

	return;
}
